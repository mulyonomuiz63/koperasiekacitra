send_notification('bendahara', [
    'title'   => 'Verifikasi Pembayaran',
    'message' => 'Ada bukti baru dari ' . $nama_anggota,
    'link'    => 'admin/pembayaran/cek/' . $invoice_id
]);

untuk cek error
data: function(d) {
                    d.tahun = $('#filter_tahun').val(); // Kirim filter tahun ke server
                    return d;
                },
                error: function(xhr, error, thrown) {
                    console.group("DEBUG DATATABLE ERROR");
                    console.log("XHR Response:", xhr.responseText); // Lihat isi error PHP di sini
                    console.log("Status:", error);
                    console.log("Error Thrown:", thrown);
                    console.groupEnd();
                }